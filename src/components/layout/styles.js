import { Box } from "@mui/system";
import { styled } from "@mui/system";

export const StyledBoxMainSection = styled(Box, {
  name: "StyledBoxMainSection",
})({
  display: "flex",
  flexDirection: "column",
  flexGrow: 1,
  justifyContent: "center",
});
